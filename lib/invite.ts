/**
 * Placeholder shown in the UI for invite links. The real URL is never rendered
 * in the DOM; it is only used when copying to the clipboard.
 */
export const INVITE_LINK_DISPLAY_PLACEHOLDER = "/accept-invite?...";
