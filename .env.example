# This file is a template.
# Copy to `.env` and replace placeholder values with actual, valid values before use.

# ====================
# App
# ====================
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_API_VERSION=v1

NEXT_PUBLIC_STORAGE_URL=http://localhost:9000

# Server-side only: direct URL to the API (RSC getServerSession, serverFetchJson).
# In production on DCISM, set to http://localhost:9055 to avoid TLS/proxy self-loop.
# BACKEND_URL=http://localhost:9055
